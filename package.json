{
  "name": "forced-multiplayer-canvas",
  "version": "1.0.0",
  "description": "A Canvas that only works when there's multiple people drawing",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrian Tennies",
  "license": "MIT",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
